import { CategoryShowPipe } from './category-show.pipe';

describe('CategoryShowPipe', () => {
  it('create an instance', () => {
    const pipe = new CategoryShowPipe();
    expect(pipe).toBeTruthy();
  });
});
